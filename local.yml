- name: ssh
  hosts: localhost
  roles:
    - ssh
  tags:
    - ssh

- name: install
  hosts: localhost
  roles:
    - install
  tags:
    - install

- name: dotfiles
  hosts: localhost
  roles:
    - dotfiles
  tags:
    - dotfiles

- name: intellihr
  hosts: localhost
  roles:
    - intellihr
  tags:
    - intellihr
